# 乱写 (Luanxie) v0.2.0 发布简介 (Release Notes)

> **发布版本**: `v0.2.0`  
> **发布日期**: 2026-09-08  
> **更新类型**: 重大功能升级、交互体系进化与全栈安全重构  
> **线上环境**: `http://8.153.74.161:8956` (服务状态: `Active: running`)

---

## 🌟 版本核心亮点 (Release Highlights)

「乱写 v0.2.0」是一次里程碑式的全方位跃升版本。本版本不仅带来了流畅跟手的**双端卡片拖拽重排**、**收件箱卡片一键直达与定位高亮**、**主题智能合并**与**子卡片置顶菜单**，更对全栈进行了**严格的安全审计与架构重构**，消除了磁盘空间泄漏与并发安全隐患，大幅精简了冗余代码，使系统在保持极简现代视觉的同时，更加健壮、优雅与高效。

---

## 🚀 新功能与交互升级 (New Features & UX)

### 1. 知识库主题与子卡片拖拽重排 (Sortable with Jelly Physics)
- **主题卡片拖拽重排**：在知识库列表长按任意主题卡片即可自由拖拽调整顺序，具备果冻物理回弹动效（`cubic-bezier` 缓动）与触觉震动反馈，全面兼容 PC 鼠标与手机端触摸操作。
- **子卡片紧凑重排模式**：点击子卡片菜单中的「排序」后，所有子卡片自动优雅收拢为单行标题卡片（`#N: 标题`），右侧提供 `☰` 抓手支持拖动调整顺序，顶部常驻「取消」与「完成」操作栏。

### 2. 子卡片操作菜单与全局置顶 (Sub-Card Actions & Pinning)
- **三点多功能菜单**：每张子卡片右上角集成现代极简三点弹出菜单，收拢「编辑内容」、「复制 Markdown」、「置顶/取消置顶」、「排序卡片」、「字数统计」以及「删除卡片」。
- **一键置顶**：置顶卡片自动浮动在主题最顶部，带有专属图钉徽标，支持多张卡片按置顶时间排序。
- **一键复制 Markdown**：点击即可快速将卡片清洗后的排版内容（含标题、正文）完整复制到系统剪贴板。

### 3. 收件箱直达卡片与脉冲高亮 (Direct Jump & Pulse Highlight)
- 在收件箱中点击关联主题链接（如 `[[目标主题]]`），系统将自动跳转至目标知识库主题，并**平滑滚动定位至该条卡片的精确位置**。
- 目标卡片自动播放 **3秒呼吸蓝色光晕脉冲动效**，并附带专属 `🎯 来自收件箱` 定位角标，大幅提升多笔记查找效率。

### 4. 主题智能合并 (Topic Merge)
- 在主题详情页底部新增「合并主题」入口，支持将当前主题全部内容迁移并入目标主题。
- 提供三种合并排序策略：
  1. **按时间排序**：保持卡片原生创建时间流插入；
  2. **移至最前**：合并入的卡片整体置于目标主题头部；
  3. **移至最后**：合并入的卡片整体追加到目标主题末尾。

### 5. 现代化悬浮胶囊 Dock 栏与极简视觉 (Modern UI)
- 底部导航栏升级为**现代悬浮胶囊 Dock 栏**，文字精炼为单字传统意象图标（`丢` / `收` / `知` / `审` / `设`），支持移动端安全区自动避让（`safe-area-inset-bottom`）。
- 去除知识库主题卡片左下角冗余的版本号指示器，页面更显纯粹清爽。

### 6. API 协议智能后缀与动态提示 (Smart API Hints & Normalization)
- 设置页中选择 API 协议（OpenAI 兼容 / Anthropic 原生）时，AI 地址标签会自动根据协议动态展示系统添加的路由后缀（如 `/v1/chat/completions` 或 `/v1/messages`）。
- 协议选择项前置，输入框占位符和帮助说明文案全流程响应式联动。

---

## 🛡️ 安全漏洞治理与稳定性加固 (Security Hardening)

| 模块 | 隐患级别 | 修复方案与效果 |
| :--- | :---: | :--- |
| **音频转写临时文件泄漏** | **高危** | 多模态转写在生成临时 16kHz PCM `*_transcribe.wav` 文件后，引入 `try ... finally` 机制，读取 Base64 发送后立即物理 `unlink` 清除，彻底消除了长期使用磁盘被爆满的高危隐患。 |
| **磁盘媒体孤儿清理** | **中危** | 将子卡片物理文件删除下沉收敛至 `server/db.py`（`delete_capture` 与 `delete_capture_and_cleanup_topic`），在 SQLite 事务提交后执行物理删除，闭环了任何删除路径下的孤儿文件滞留。 |
| **登录防爆破并发竞争态** | **中危** | `server/routes/auth.py` 中的 `_login_locked(ip)` 严格加入 `_fail_lock` 互斥锁，消除了并发扫描爆破时的锁穿透竞态。 |
| **Base URL 语音 404 缺陷** | **中危** | 统一收敛 `server/_net.py` 中的 `normalize_base_url`，解决了音频转写 `transcribe.py` 中因用户填入根域名导致拼接 404 的逻辑缺陷。 |

---

## ⚡ 性能优化与架构优雅度重构 (Performance & Architecture)

### 1. SQLite 复合索引加速
- 针对知识库子卡片列表高频查询，新增复合索引：
  ```sql
  CREATE INDEX idx_captures_topic_sort ON captures(topic_id, is_pinned DESC, sort_order ASC, created_at ASC);
  ```
  避免了数据量增长后的全表扫描与内存文件排序（filesort），秒级渲染百条卡片主题。

### 2. 待确认队列 N+1 数据库查询消除
- 将 `server/routes/review.py` 中循环内部单条串行查询主题标题的机制，重构为基于去重 ID 集合的批量内存映射注入，彻底消除数据库网络往返开销。

### 3. 前端组件解耦与代码优雅度跃升
- **独立组件 `AudioPlayButton.tsx`**：将 `TopicDetail.tsx` 内嵌的长达 140 行音频/原图播放逻辑抽离为独立组件，主页面渲染更纯粹。
- **通用组件 `ModelGroupCard.tsx`**：将 `SettingsPage.tsx` 中文字、图像、音频、合并 4 组完全相同的 260 行复制粘贴 JSX 抽象为统一数据驱动驱动循环，**净削减 230+ 行重复代码**。
- **语义化样式**：将 JS 内嵌样式迁移并入 `web/src/styles.css`，减少浏览器运行时内存对象分配。

---

## 📊 版本工程指标 (Release Metrics)

- **Git 提交**: `a1243b9`
- **代码变动**: 15 个文件变更，新增 430 行，删除 494 行（净精简代码 **-64 行**）
- **前端编译**: TypeScript 严格模式校验通过，Vite 打包耗时 **2.45s**，产物零告警
- **后端服务**: FastAPI + Uvicorn 原生单进程低延迟运行，内存占用稳定在 **~73MB**
- **健康检查**: `curl http://127.0.0.1:8787/api/health` 响应正常，队列深度为 0

---

*Luanxie v0.2.0 · 让记录如随手乱写般轻盈，让沉淀如知识结晶般坚实。*
